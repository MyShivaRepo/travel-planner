services:
  travel-planner:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: travel-planner
    ports:
      - "9999:9999"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - DB_PATH=/data/travel_planner.db
    volumes:
      - travel-data:/data
    restart: unless-stopped

volumes:
  travel-data:
